# Changelog

Все значительные изменения в проекте документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
и проект придерживается [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.6.0] - 2025-10-09

### Added

- **Динамическое окно для клонирования (Подход 9):**
  - Одно окно `confirm_clone` с тремя динамическими состояниями
  - Использование `when=F[...]` условий для переключения между состояниями
  - Состояния: подтверждение → прогресс → результат
  - Сообщение редактируется вместо создания нового
  - Документация в `docs/aiogram_dialog.md`

- **Интеграция BgManager в фоновую задачу:**
  - Функция `clone_project_background_with_manager()` с BgManager
  - Использование `manager.update()` вместо прямого Bot API
  - Callback функции для обновления прогресса через BgManager
  - Handler `on_message_during_clone` для игнорирования сообщений во время клонирования

- **Стандартный Progress виджет:**
  - Замена ручного progress bar на `Progress("progress", 10)`
  - Автоматическое отображение процентов (убран ручной вывод)
  - Встроенный рендеринг прогресс-бара

### Changed

- **Документация aiogram_dialog.md:**
  - Полное переписывание с фокусом на Progress виджет
  - Описание проблемы: `switch_to()` создает новые сообщения
  - Решение: динамическое окно (подход 9)
  - Примеры реализации из `clone_project.py`
  - Описание всех неработающих подходов (1-8)
  - Чеклист для динамических окон

- **Оптимизация bot/states.py:**
  - Удалены неиспользуемые состояния `CloneProject.progress` и `CloneProject.result`
  - Обновлен комментарий для `confirm_clone` (динамическое окно)

### Removed

- **Очистка неиспользуемого кода в clone_project.py:**
  - Окно 6 (Progress) и окно 7 (Result) - заменены динамическим окном
  - Getters: `get_progress_data()`, `get_result_data()`
  - Handler: `clone_project_background_simple()` (175 строк с прямым Bot API)
  - Handler: `on_finish()`
  - Импорты: `InlineKeyboardMarkup`, `InlineKeyboardButton`, `StartMode`, `MainMenu`, `Multi`, `create_progress_callback`, `typing.Any`, `typing.Dict`
  - Сокращение с 819 до 560 строк (-259 строк, -32%)

- **Удаление тестовых компонентов:**
  - Файл `bot/dialogs/test_progress_dialog.py` (тестовый диалог с 12 подходами)
  - Файл `bot/dialogs/clone_project.py.backup`
  - Класс `TestProgress` из `bot/states.py`
  - Команда `/test` из `bot/handlers/commands.py`
  - Импорты и регистрация `test_progress_dialog` из `__init__.py` и `main.py`

- **Удаление устаревшей документации:**
  - `docs/PROGRESS_WIDGET_INTEGRATION.md` (временный анализ)
  - `docs/LOGGING.md` (по запросу пользователя)

### Fixed

- **Логическая ошибка в clone_project.py:553:**
  - Было: `when=~F["is_cloning"] & ~F["error"] & F["error"]` (всегда False)
  - Стало: `when=~F["is_cloning"] & ~F["result"] & F["error"]`
  - Исправлено отображение сообщения об ошибке

## [0.5.0] - 2025-10-08

### Added

- **Кэширование данных для пагинации:**
  - Кэширование списка проектов-шаблонов в `dialog_data["template_projects"]`
  - Кэширование списка очередей в `dialog_data["cached_queues"]`
  - Запросы к API выполняются только один раз при входе на окно
  - Кэш автоматически очищается при завершении диалога

- **Документация альтернативного подхода (docs/aiogram.md):**
  - Раздел "Прямое использование Bot API (без BgManager)"
  - Описание всех проблем с BgManager (IncorrectBackgroundError, UnknownIntent)
  - Примеры с InlineKeyboardMarkup и обработчиками callback
  - Рекомендации по выбору подхода

- **Финальное окно клонирования:**
  - InlineKeyboard с кликабельными кнопками
  - Кнопка "🔗 Открыть проект" - прямая ссылка в Tracker
  - Кнопка "🏠 Главное меню" - возврат в главное меню
  - Обработчик `goto_main_menu` в commands_router

### Changed

- **Прогресс клонирования:**
  - Объединен в единую шкалу 0-100%:
    - 0-50% - получение данных (fetch_project_data)
    - 50-100% - клонирование (clone_project)
  - Добавлено процентное отображение рядом с progress bar
  - Убран текст "⏳ Пожалуйста, подождите..."

- **Реализация фоновой задачи:**
  - Полностью без DialogManager в фоновой задаче
  - Прямое редактирование сообщения через `bot.edit_message_text()`
  - Формирование progress bar вручную: `"▓" * filled + "░" * (10 - filled)`
  - Результат отображается через InlineKeyboardMarkup в том же сообщении

- **Формат ссылки на проект:**
  - Изменен с `https://tracker.yandex.ru/{shortID}`
  - На `https://tracker.yandex.ru/pages/projects/{shortID}`

### Fixed

- **Проблемы с BgManager в фоновой задаче:**
  - IncorrectBackgroundError при доступе к `manager.dialog_data`
  - UnknownIntent при использовании `bg.start()` с разными stack_id
  - Возврат UI на предыдущее окно вместо прогресса
  - Решение: отказ от BgManager в пользу прямого Bot API

- **Дублирование логов YaTrackerApi:**
  - Установлен `ya_tracker_logger.propagate = False`
  - Создана документация `docs/LOGGING.md` с объяснением проблемы
  - Установлен `log_level="WARNING"` по умолчанию в TrackerClient

- **Telegram ID overflow:**
  - Изменен тип `telegram_id` с Integer на BigInteger
  - Поддержка ID больше 2,147,483,647

- **Поддержка внешней БД:**
  - Удалены хардкоженные credentials из `database.py`
  - DATABASE_URL обязателен в .env с подробным сообщением об ошибке
  - Создан `docker-compose.bot-only.yml` для подключения к существующей БД
  - Документация в `docs/DOCKER.md`

- **RESET_DB для Docker:**
  - Добавлена переменная окружения RESET_DB=true
  - Автоматический сброс БД при запуске контейнера
  - Продолжение работы бота после сброса

## [0.4.0] - 2025-10-08

### Added

- **Docker-контейнеризация:**
  - `Dockerfile` для сборки образа бота (Python 3.11-slim + uv)
  - `docker-compose.yml` с PostgreSQL 15 и ботом
  - `.dockerignore` для оптимизации размера образа
  - Healthcheck для PostgreSQL
  - Логирование с ротацией (max 10MB, 3 файла)
  - Автоматическая инициализация БД при первом запуске
  - Документация `docs/DOCKER.md` с полным руководством

- **Автоматическая инициализация БД:**
  - Функция `init_default_owners()` для создания владельцев из .env
  - Интеграция в `main.py` - автоматический запуск при старте
  - Поддержка 1-2 владельцев (OWNER2 опционально)
  - Проверка существования для предотвращения дублирования

- **Опции командной строки:**
  - `--reset-db` - показать предупреждение о сбросе БД
  - `--reset-db --confirm` - удалить и пересоздать все таблицы
  - `--help` - справка по всем опциям

- **Улучшения UI клонирования:**
  - Отображение человекочитаемых названий проектов вместо ID
  - Прямая ссылка на созданный проект в результатах
  - Кнопка "🔗 Открыть проект" с URL в Yandex Tracker
  - Сохранение shortId и имени проекта в результатах

### Changed

- **Обновлен `.env.example`:**
  - Полный набор всех требуемых переменных
  - Комментарии для Docker и локального запуска
  - Примеры значений для всех полей

- **Обновлен `.gitignore`:**
  - Расширен список исключений для Python проектов
  - Добавлены правила для Windows/macOS/Linux
  - Исключения для IDE, тестов, coverage
  - Исключения для временных файлов

- **Обновлен `README.md`:**
  - Полное переписывание под Telegram бота
  - Docker-first подход в инструкциях
  - Badges (Python, Docker, License)
  - Таблица ролевой системы
  - Структура проекта
  - Секция "Разработка" и "Вклад"

### Removed

- `init_db.py` - больше не требуется (встроено в main.py)
- `drop_and_init_db.py` - заменено на `--reset-db --confirm`
- Все тестовые скрипты и файлы:
  - `test_clone.py`
  - `test_project_copy.py`
  - `bot/dialogs/test_progress.py`
  - `bot/dialogs/test_progress_v3.py`
  - `TEST_PROJECT_COPY.md`
  - `debug.txt`

### Fixed

- **Клонирование полей проекта (Lead, TeamUsers, ParentEntity, TeamAccess):**
  - Обновлен YaTrackerApi до версии 2.1.3
  - Исправлена обработка объектов пользователей (извлечение `id` вместо `login`)
  - Исправлено преобразование вложенной структуры `parentEntity`
  - Добавлена проверка полей в объекте `fields`
  - Документированы особенности API в `docs/YaTrackerApi.md` (разделы 7-11)

- **Real-time обновление прогресса:**
  - Правильное использование `bg.start()` с `stack_id`
  - Документация в `docs/aiogram.md` по работе с BgManager
  - Прогресс теперь обновляется в реальном времени

## [0.3.0] - 2025-01-08

### Added

- **Отображение ФИО пользователей:**
  - Поле `display_name` в модель User для хранения ФИО из Tracker API
  - Автоматическое сохранение display_name при выборе пользователя из Tracker
  - Формат списка: `{role_emoji} {ФИО}` (👑/📊/👷 + имя)
  - Настройка display_name для владельцев через .env (OWNER1_DISPLAY_NAME, OWNER2_DISPLAY_NAME)

- **Выбор очереди из списка API:**
  - В диалоге клонирования очередь выбирается из списка (как в настройках)
  - Если есть очередь по умолчанию - предлагается использовать или выбрать другую
  - ScrollingGroup + Select с форматом: `{name} ({key})`

### Changed

- **Главное меню:**
  - Упрощенное приветствие: "Привет, {ФИО}" (без заголовка бота и роли)
  - Удалена кнопка "ℹ️ Информация о проекте"
  - Изменен порядок кнопок: "Управление пользователями" выше "Настройки"

- **Ролевой доступ:**
  - Кнопка "Клонировать проект" доступна только owners и managers
  - Кнопка "Настройки" скрыта для workers
  - Настройки workers: только очередь (без портфеля)
  - Настройки managers/owners: очередь + портфель

- **Диалог клонирования:**
  - Отображаются человекочитаемые названия проектов вместо ID
  - ID убран из окна подтверждения проекта
  - Добавлен ShowMode.EDIT для всех переходов (редактирование сообщения)
  - Убраны сообщения-подтверждения при вводе данных

- **Инициализация БД:**
  - Владельцы создаются по username вместо telegram_id
  - Требуется указать OWNER1_USERNAME, OWNER2_USERNAME в .env
  - Требуется указать OWNER1_DISPLAY_NAME, OWNER2_DISPLAY_NAME в .env

### Fixed

- BgManager теперь использует параметры вместо прямого доступа к dialog_data
- Данные результата клонирования сохраняются в middleware_data

## [0.2.0] - 2025-01-08

### Added

- **Ролевая система авторизации:**
  - База данных SQLite с SQLAlchemy
  - Модели `User`, `UserSettings`, `UserRole`
  - 3 роли: Owner (полный доступ), Manager (копирование), Worker (резерв)
  - Middleware `AuthMiddleware` для проверки авторизации
  - CRUD операции через `UserCRUD`

- **Диалоги управления (Single Window Interface):**
  - `user_management.py` - CRUD пользователей (только Owner)
  - `user_settings.py` - настройки пользователя (очередь, портфель)
  - Single window архитектура с `dialog_data["mode"]` и `dialog_data["step"]`
  - MessageInput с `message.delete()` для инпутов без создания новых сообщений

- **Настройки по умолчанию:**
  - Очередь: ZADACIBMT
  - Портфель: 65cde69d486b9524503455b7
  - Возможность изменения через диалог настроек

- **Инициализация:**
  - Скрипт `init_db.py` для создания БД и владельцев
  - Автоматическая инициализация БД при запуске бота
  - Поддержка конфигурации владельцев через .env

- **Зависимости:**
  - sqlalchemy 2.0.43
  - asyncpg 0.30.0
  - alembic 1.16.5

### Changed

- Главное меню теперь показывает роль пользователя
- Кнопки в меню отображаются в зависимости от роли (when условия)
- `main.py` теперь инициализирует БД и регистрирует middleware
- **База данных переведена с SQLite на PostgreSQL** (asyncpg вместо aiosqlite)
- DATABASE_URL теперь настраивается через .env

### Added (старое)

- Базовая структура проекта
- `TrackerClient` - обертка над YandexTrackerClient с загрузкой из .env
- `ProjectCloner` - класс для полного клонирования проектов
- Поддержка Progress callback для интеграции с aiogram-dialog
- Методы получения данных проекта:
  - `fetch_project_data()` - получение всех данных проекта
  - `_fetch_project_issues()` - получение задач
  - `_fetch_all_checklists()` - получение чеклистов
  - `_fetch_all_links()` - получение связей между задачами
  - `_fetch_all_comments()` - получение комментариев
- Методы клонирования:
  - `clone_project()` - создание полной копии проекта
  - `_create_project_copy()` - создание проекта
  - `_clone_issues()` - клонирование задач
  - `_restore_checklists()` - восстановление чеклистов
  - `_restore_links()` - восстановление связей
  - `_restore_comments()` - восстановление комментариев
- Dataclasses:
  - `ProjectData` - структура данных проекта
  - `CloneResult` - результат клонирования
- Утилиты для работы с aiogram-dialog:
  - `update_progress_in_dialog()` - обновление прогресса
  - `create_progress_callback()` - создание callback функции
  - `format_project_summary()` - форматирование информации
  - `format_clone_result()` - форматирование результата
- Примеры использования:
  - `examples/simple_usage.py` - простое использование
  - `examples/aiogram_dialog_integration.py` - интеграция с ботом
- Документация:
  - `docs/TRANSIT.md` - текущее состояние проекта
  - `docs/CLASSES_and_FILES.md` - структура классов и файлов
  - `docs/TECH.md` - технический стек и архитектура
  - `docs/YaTrackerApi.md` - документация API

### Changed

- N/A

### Deprecated

- N/A

### Removed

- N/A

### Fixed

- N/A

### Security

- N/A

## [0.1.0] - TBD

Первый релиз проекта.
