# TRANSIT - Текущее состояние проекта

## Концепция проекта

YaTackerHelper - Telegram бот для полного клонирования проектов из Yandex Tracker со всеми связанными сущностями (задачи, подзадачи, чеклисты, связи, комментарии) с визуализацией прогресса через aiogram-dialog.

## Текущий статус - UX УЛУЧШЕНИЯ РЕАЛИЗОВАНЫ

### ✅ Реализовано

1. **Модуль клонирования (src/)**
   - `tracker_client.py` - обертка над YandexTrackerClient с автозагрузкой из .env
   - `project_cloner.py` - класс с рекурсивным обходом подзадач и связей
   - `utils.py` - утилиты для работы с aiogram-dialog Progress

2. **Функциональность ProjectCloner**
   - ✅ Рекурсивный обход подзадач через связи
   - ✅ Проверка и дополнение связанных задач
   - ✅ Сохранение иерархии parent-child
   - ✅ Восстановление всех сущностей (чеклисты, связи, комментарии)
   - ✅ Детальный прогресс с фазами выполнения

3. **Telegram бот (bot/)**
   - `main.py` - точка входа бота с инициализацией БД и middleware
   - `config.py` - загрузка конфигурации из .env
   - `states.py` - FSM состояния для всех диалогов
   - `handlers/commands.py` - обработка /start, /help, /cancel
   - **Диалоги:**
     - `dialogs/main_menu.py` - главное меню с ролевым доступом
     - `dialogs/clone_project.py` - полный диалог клонирования с Progress
     - `dialogs/project_info.py` - просмотр информации о проекте
     - `dialogs/user_management.py` - управление пользователями (CRUD, только Owner)
     - `dialogs/user_settings.py` - настройки пользователя (очередь, портфель)

4. **Ролевая система**
   - **База данных (PostgreSQL + SQLAlchemy + asyncpg)**
     - `bot/database/models.py` - User, UserSettings, UserRole
     - `bot/database/database.py` - подключение, сессии, init_db
     - `bot/database/crud.py` - UserCRUD для всех CRUD операций
   - **Роли:**
     - `OWNER` - управление пользователями + копирование проектов
     - `MANAGER` - копирование проектов
     - `WORKER` - только настройки очереди
   - **Авторизация:**
     - `bot/middlewares/auth.py` - проверка пользователя в БД перед любым действием
     - Блокировка неавторизованных пользователей
   - **Настройки по умолчанию:**
     - Очередь: ZADACIBMT
     - Портфель: 65cde69d486b9524503455b7
   - **Single Window Interface:**
     - Все диалоги управления используют один window с dialog_data["mode"] и dialog_data["step"]
     - MessageInput с message.delete() для инпутов
     - Динамический контент через when условия

5. **UX улучшения (NEW!)**
   - **Отображение пользователей:**
     - Добавлено поле `display_name` в модель User (хранит ФИО из Tracker API)
     - Формат списка пользователей: `{role_emoji} {ФИО}`
     - Автоматическое сохранение display_name при выборе пользователя из Tracker API
   - **Главное меню:**
     - Упрощенное приветствие: "Привет, {ФИО}" (без заголовка бота и роли)
     - Удалена кнопка "Информация о проекте"
     - Ролевой доступ к функциям:
       - "Клонировать проект" - только owners и managers
       - "Управление пользователями" - только owners
       - "Настройки" - только owners и managers
   - **Настройки пользователя:**
     - Для workers: доступна только настройка очереди
     - Для managers/owners: доступны очередь + портфель
   - **Диалог клонирования:**
     - Человекочитаемые названия проектов вместо ID
     - Выбор очереди из списка API (как в настройках) вместо ручного ввода
     - Если есть очередь по умолчанию - предлагает использовать или выбрать другую
     - ShowMode.EDIT для всех переходов (редактирование сообщения вместо создания нового)

6. **Автоматическая инициализация БД**
   - При запуске бота автоматически создаются таблицы
   - Автоматическое создание владельцев из .env при первом запуске
   - Требуется указать OWNER1_USERNAME, OWNER1_TRACKER_LOGIN, OWNER1_DISPLAY_NAME
   - Опционально: OWNER2_USERNAME, OWNER2_TRACKER_LOGIN, OWNER2_DISPLAY_NAME
   - Автоматическое создание настроек для каждого пользователя
   - Функции: `bot/database/database.py:init_db()` и `init_default_owners()`


### ✅ Недавно решено

**2025-10-09: Интеграция подхода 9 (динамическое окно) и очистка кода**
- **Проблема:** `switch_to()` между окнами создавал новые сообщения вместо редактирования
- **Решение:** Подход 9 - динамическое окно с условиями `when`
  - Одно окно `confirm_clone` с тремя состояниями:
    1. Подтверждение (`is_cloning=False, result=None`)
    2. Прогресс (`is_cloning=True`)
    3. Результат (`is_cloning=False, result` есть)
  - Виджеты показываются/скрываются через `when=F[...]` условия
  - Handler НЕ делает `switch_to()` - остается на том же окне
  - Фоновая задача использует `manager.update()` для обновления данных
  - Окно само перерисовывается на основе изменения данных
  - **Результат:** Сообщение редактируется, а не создается новое!
- **Интеграция BgManager:**
  - Используется `manager.bg()` вместо прямого Bot API
  - Функция `clone_project_background_with_manager()` с DialogManager
  - Обновление через `manager.update()` изменяет данные и триггерит перерисовку
  - Стандартный `Progress("progress", 10)` виджет вместо ручного bar
- **Очистка кода:**
  - Удалены неиспользуемые окна Progress и Result
  - Удалены getters: `get_progress_data()`, `get_result_data()`
  - Удалена функция `clone_project_background_simple()` (175 строк с прямым Bot API)
  - Удален handler `on_finish()`
  - Очищены импорты (InlineKeyboardMarkup, StartMode, MainMenu, Multi, и др.)
  - Удалены состояния `CloneProject.progress` и `CloneProject.result`
  - Сокращение `clone_project.py` с 819 до 560 строк (-259 строк, -32%)
- **Удаление тестовых компонентов:**
  - Удален `bot/dialogs/test_progress_dialog.py` (тестовый диалог с 12 подходами)
  - Удален класс `TestProgress` из `bot/states.py`
  - Удалена команда `/test` из `bot/handlers/commands.py`
  - Удалены импорты и регистрация `test_progress_dialog`
- **Документация:**
  - Полное переписывание `docs/aiogram_dialog.md` с фокусом на Progress виджет
  - Описание проблемы и решения (подход 9)
  - Примеры реализации из `clone_project.py`
  - Описание всех неработающих подходов (1-8)
  - Чеклист для динамических окон
- **Исправления:**
  - Логическая ошибка в условии отображения ошибки (строка 553)
- Файлы: `bot/dialogs/clone_project.py`, `bot/states.py`, `bot/handlers/commands.py`, `docs/aiogram_dialog.md`, `docs/CHANGELOG.md`

**2025-10-08: Исправлено дублирование логов YaTrackerApi**
- Проблема: логи выводились дважды (библиотека имеет свой handler + propagate в root logger)
- Причина: YaTrackerApi.logger.propagate=True передавал логи в root logger от basicConfig
- Решение: отключен propagate через `ya_tracker_logger.propagate = False`
- Дополнительно: установлен log_level="WARNING" для уменьшения шума в production
- Создана документация: `docs/LOGGING.md` с подробным объяснением
- Файлы: `main.py:34-39`, `src/tracker_client.py:15, 23`, `docs/LOGGING.md`

**2025-10-08: Исправлены UX проблемы в диалоге клонирования**
- Прогресс клонирования теперь обновляет текущее сообщение вместо создания нового
- Изменено: `bg.start()` → `manager.switch_to()` для перехода к окну прогресса
- После нажатия "Готово" корректно возвращает в главное меню
- Добавлен автоматический переход: `manager.done()` + `manager.start(MainMenu.main)`
- Файлы: `bot/dialogs/clone_project.py:300, 417-419`

**2025-10-08: Добавлена переменная окружения RESET_DB для Docker**
- Теперь можно сбрасывать БД через переменную окружения `RESET_DB=true`
- Удобно для Docker: не нужно входить в контейнер для выполнения команд
- Автоматически выполняет сброс при запуске контейнера
- ⚠️ После запуска нужно удалить RESET_DB из .env!
- Файлы: `main.py:157-168`, `.env.example:39-46`, `docs/DOCKER.md`

**2025-10-08: Исправлена ошибка с большими Telegram ID**
- Изменен тип поля `telegram_id` с `Integer` (int32) на `BigInteger` (int64)
- Проблема: Telegram ID больше 2,147,483,647 вызывали ошибку "value out of int32 range"
- Решение: BigInteger поддерживает значения до 9,223,372,036,854,775,807
- Для существующих БД: установи `RESET_DB=true` в .env или выполни `uv run python main.py --reset-db --confirm`
- Файлы: `bot/database/models.py:32`

**2025-10-08: Добавлена поддержка подключения к существующей БД**
- Создан `docker-compose.bot-only.yml` для запуска только бота
- Обновлен `.env.example` с примерами для разных сценариев подключения
- Обновлен `docs/DOCKER.md` с подробной документацией:
  - Секция "Подключение к существующей БД"
  - Примеры DATABASE_URL для Windows/Mac/Linux
  - Примеры для локальной и удаленной БД
  - Команды управления для bot-only версии
- Файлы: `docker-compose.bot-only.yml`, `.env.example`, `docs/DOCKER.md`

**2025-10-08: Подготовка к публикации на GitHub**
- Обновлен .gitignore с расширенным списком исключений
- Полностью переписан README.md под Telegram бота с Docker
- Добавлен MIT License
- Обновлен CHANGELOG.md с версией 0.4.0
- Создан requirements.txt для пользователей без uv
- Обновлен pyproject.toml (версия 0.4.0, описание, автор, GitHub URLs)
- Проект готов к публикации на https://github.com/imdeniil/YaTackerHelper

**2025-10-08: Добавлена Docker-контейнеризация**
- Создан Dockerfile для сборки образа бота
- Создан docker-compose.yml с PostgreSQL и ботом
- Добавлен .dockerignore для оптимизации образа
- Обновлен .env.example с полным набором переменных
- Автоматическая инициализация БД при первом запуске
- Healthcheck для PostgreSQL
- Логирование с ротацией
- Файлы: `Dockerfile`, `docker-compose.yml`, `.dockerignore`

**2025-10-08: Автоматизирована инициализация БД**
- Удален скрипт `init_db.py` - больше не требуется ручная инициализация
- При запуске бота автоматически:
  - Создаются таблицы в БД
  - Создаются владельцы из переменных окружения .env
  - Проверяется существование владельцев (не дублируются)
- Добавлена опция `--reset-db --confirm` для сброса БД (встроена в main.py)
- Функции: `bot/database/database.py:init_db()` и `init_default_owners()`
- Изменения: `bot/database/database.py:31-94`, `main.py:34-67, 130-158`

**2025-10-08: Клонирование полей проекта исправлено!**
- Проблема: При клонировании не копировались Lead, TeamUsers, ParentEntity, TeamAccess
- Решение:
  - Обновлен YaTrackerApi до версии 2.1.3
  - Исправлена работа с особенностями API (см. `docs/YaTrackerApi.md` разделы 7-11):
    - Извлечение `id` вместо `login` из объектов пользователей
    - Преобразование вложенной структуры `parentEntity`
    - Проверка полей в `fields` объекте
    - Формат `parent_entity` как словарь `{"primary": "id", "secondary": []}`
    - Использование `team_access` в `create()`
- Изменения: `src/project_cloner.py:347-456`
- Документация: `docs/YaTrackerApi.md` (разделы 7-11)

**2025-01-08: Real-time обновление прогресса исправлено!**
- Проблема: BgManager не обновлял UI в реальном времени
- Решение: Правильное использование `bg.start()` с `stack_id`
- Детали: См. `docs/aiogram.md` - полная документация по работе с BgManager

**Ключевые изменения:**
```python
# Правильный подход
bg = manager.bg(
    user_id=callback.from_user.id,
    chat_id=callback.message.chat.id,
    stack_id="unique_stack_id",
    load=True,
)
await bg.start(State.progress, mode=StartMode.RESET_STACK)
asyncio.create_task(background_task(bg))
```

### ✅ Оптимизации

**2025-10-08: Обновлена документация aiogram.md**
- Добавлен раздел "Альтернативный подход: Прямое использование Bot API (без BgManager)"
- Описаны все проблемы с BgManager (IncorrectBackgroundError, UnknownIntent, возврат UI)
- Добавлены полные примеры с InlineKeyboardMarkup и обработчиками callback
- Описаны преимущества и недостатки подхода без BgManager
- Добавлены рекомендации, когда использовать каждый подход
- Файл: `docs/aiogram.md:308-492`

**2025-10-08: Добавлено кэширование данных для пагинации**
- Проблема: При переключении страниц пагинатора делались повторные запросы к API
- Решение: Кэширование данных в dialog_data до смены окна
  - Проекты-шаблоны: кэш в `dialog_data["template_projects"]`
  - Очереди: кэш в `dialog_data["cached_queues"]`
  - Кэш автоматически очищается при завершении диалога (manager.done())
- Результат: Запросы к API выполняются только один раз при входе на окно
- Файлы: `bot/dialogs/clone_project.py:21-104, 127-164`

### 🔄 Ближайшие шаги

1. **Будущие улучшения:**
   - Добавить обработку вложений (attachments)
   - Улучшить обработку ошибок с детальным логированием
   - Добавить выборочное клонирование (фильтр задач)
   - Добавить настройку портфеля по умолчанию в диалоге клонирования
   - Оптимизация производительности для больших проектов

## Ключевые файлы

### Клонирование
- `src/project_cloner.py:108-136` - рекурсивный сбор задач с подзадачами
- `src/project_cloner.py:138-190` - рекурсивный обход иерархии
- `src/project_cloner.py:192-225` - проверка связанных задач
- `src/project_cloner.py:438-469` - восстановление parent-child связей

### Бот
- `bot/main.py` - инициализация и запуск
- `bot/dialogs/clone_project.py:22-105` - загрузка проектов-шаблонов через API (с кэшированием)
- `bot/dialogs/clone_project.py:197-221` - обработка выбора проекта
- `bot/dialogs/clone_project.py:277-306` - запуск фоновой задачи через BgManager (без switch_to!)
- `bot/dialogs/clone_project.py:317-420` - фоновая задача клонирования с BgManager и manager.update()
- `bot/dialogs/clone_project.py:507-564` - динамическое окно с тремя состояниями (подход 9)
- `docs/aiogram_dialog.md` - документация по Progress виджету и динамическим окнам

### Языки запросов
- `docs/YaTrackerQuery.md` - полная документация языка запросов Yandex Tracker
- Используется запрос: `Summary: "шаблон"` для поиска проектов

## Как запустить

### Вариант 1: Docker (Рекомендуется)

#### 1. Подготовка

Скопируй `.env.example` в `.env` и заполни своими значениями:

```bash
cp .env.example .env
```

Отредактируй `.env`:
```env
# Yandex Tracker API
TRACKER_API_KEY=y0_...
TRACKER_ORG_ID=674252

# Telegram Bot
BOT_TOKEN=7498622514:...

# Database - используй это значение для Docker
DATABASE_URL=postgresql+asyncpg://yatrackerhelper:changeme@postgres:5432/yatrackerhelper

# Пароль для PostgreSQL (опционально, по умолчанию: changeme)
DB_PASSWORD=your_secure_password

# Владельцы бота
OWNER1_USERNAME=iVars_b
OWNER1_TRACKER_LOGIN=concept-rp
OWNER1_DISPLAY_NAME=Айварс Балиньш

OWNER2_USERNAME=imdeniil
OWNER2_TRACKER_LOGIN=imdeniil
OWNER2_DISPLAY_NAME=Даниил Павлючик
```

#### 2. Запуск

```bash
# Запуск в фоне
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot

# Остановка
docker-compose down

# Остановка с удалением данных БД
docker-compose down -v
```

**Что происходит:**
1. ✅ Создается контейнер PostgreSQL
2. ✅ Создается контейнер с ботом
3. ✅ Автоматически создаются таблицы
4. ✅ Автоматически создаются владельцы из .env
5. ✅ Бот запускается и готов к работе

### Вариант 2: Локальный запуск

#### 1. Настройка переменных окружения

Создай файл `.env` в корне проекта:

```env
# Yandex Tracker API
TRACKER_API_KEY="y0_..."
TRACKER_ORG_ID="674252"

# Telegram Bot
BOT_TOKEN="7498622514:..."

# Database (PostgreSQL)
DATABASE_URL="postgresql+asyncpg://user:password@host:port/database"

# Владельцы бота (обязательно OWNER1, OWNER2 опционально)
OWNER1_USERNAME="iVars_b"
OWNER1_TRACKER_LOGIN="concept-rp"
OWNER1_DISPLAY_NAME="Айварс Балиньш"

OWNER2_USERNAME="imdeniil"
OWNER2_TRACKER_LOGIN="imdeniil"
OWNER2_DISPLAY_NAME="Даниил Павлючик"
```

### 2. Запуск бота

```bash
# Обычный запуск
uv run python main.py

# Сброс базы данных (удалит все данные!)
uv run python main.py --reset-db --confirm

# Справка по опциям
uv run python main.py --help
```

**Что происходит при первом запуске:**
1. ✅ Автоматически создаются таблицы в БД
2. ✅ Автоматически создаются владельцы из .env
3. ✅ При первом входе владельцев в бота, их telegram_id привязывается к аккаунту
4. ✅ Автоматически создаются настройки для каждого пользователя

**Опции командной строки:**
- `--reset-db` - показать предупреждение о сбросе БД
- `--reset-db --confirm` - удалить и пересоздать все таблицы БД (⚠️ удалит все данные!)
- `--help` - показать справку по всем опциям
