# YaTackerHelper Web Interface

Веб-интерфейс на FastHTML для системы управления запросами на оплату.

## Особенности

- 🔐 Авторизация через Telegram Login Widget
- 🎭 Поддержка ролей (Owner, Manager, Worker)
- 💰 Управление запросами на оплату
- 📁 Хранение файлов через Telegram Bot API
- ⚡ Real-time обновления через WebSockets
- 🎨 Современный UI на DaisyUI + TailwindCSS
- 🔄 Общая база данных с телеграм-ботом

## Структура

```
web/
├── app.py              # Основное приложение FastHTML
├── config.py           # Конфигурация
├── database.py         # Подключение к БД (переиспользует модели бота)
├── routes/            # Маршруты приложения
│   ├── auth.py        # Авторизация через Telegram
│   ├── dashboard.py   # Dashboard по ролям
│   └── ...
└── static/            # Статические файлы
```

## Установка и настройка

### 1. Установка зависимостей

Зависимости уже включены в pyproject.toml. Выполните:

```bash
uv sync
```

### 2. Настройка .env

Добавьте в `.env` файл следующие переменные:

```bash
# Секретный ключ для сессий (сгенерируйте случайный)
WEB_SECRET_KEY=your_random_secret_key_here

# Порт для веб-приложения (по умолчанию 8000)
WEB_PORT=8000

# Host для веб-приложения (по умолчанию 0.0.0.0)
WEB_HOST=0.0.0.0

# ID приватной группы для хранения файлов
STORAGE_CHAT_ID=-1001234567890
```

#### Как получить STORAGE_CHAT_ID:

1. Создайте приватную группу в Telegram
2. Добавьте вашего бота в эту группу как администратора
3. Отправьте любое сообщение в группу
4. Откройте в браузере:
   ```
   https://api.telegram.org/bot<BOT_TOKEN>/getUpdates
   ```
5. Найдите `chat.id` в ответе (отрицательное число)

#### Генерация WEB_SECRET_KEY:

```bash
python -c "import secrets; print(secrets.token_hex(32))"
```

### 3. Настройка Telegram Login Widget

В файле `web/routes/auth.py` укажите имя вашего бота в параметре `data_telegram_login`:

```python
data_telegram_login="bmyatrackerv2bot"  # Замените на имя вашего бота
```

⚠️ **Важно**: Убедитесь, что в настройках вашего бота (@BotFather) указан домен для авторизации:

```
/setdomain @your_bot_name
yourdomain.com
```

Для локального тестирования можно использовать `localhost` или `127.0.0.1`.

## Запуск

### Локальный запуск

```bash
python run_web.py
```

Или напрямую:

```bash
python -m web.app
```

Веб-интерфейс будет доступен по адресу: `http://localhost:8000`

### Запуск вместе с ботом

Бот и веб-приложение запускаются **отдельными процессами**:

**Терминал 1 - Бот:**
```bash
python main.py
```

**Терминал 2 - Веб:**
```bash
python run_web.py
```

Оба используют **общую базу данных** и **общие модели**.

## Архитектура

### Общая БД с ботом

Веб-приложение переиспользует:
- ✅ Модели SQLAlchemy из `bot/database/models.py`
- ✅ CRUD операции из `bot/database/crud.py`
- ✅ Enum'ы (UserRole, PaymentRequestStatus)

Создается **отдельный engine и session maker**, но подключение к **той же БД**.

### Авторизация

1. Пользователь нажимает "Login with Telegram"
2. Telegram Login Widget возвращает данные + hash
3. Сервер проверяет подлинность hash (HMAC-SHA256)
4. Поиск пользователя в БД по `telegram_id`
5. Создание сессии (cookies) с user_id и ролью
6. Редирект на соответствующий dashboard

### Система ролей

**Worker:**
- Создание запросов на оплату
- Просмотр своих запросов
- Получение уведомлений об изменениях

**Billing Contact** (любая роль + флаг):
- Просмотр PENDING запросов
- Оплата запросов (загрузка платежки)
- Планирование оплаты
- Отмена запросов

**Owner/Manager:**
- Все функции Billing Contact
- Просмотр всех запросов системы
- Управление пользователями (только Owner)
- Расширенная аналитика

## Roadmap

- [x] Базовая структура приложения
- [x] Авторизация через Telegram
- [x] Подключение к общей БД
- [x] Базовый dashboard
- [ ] Dashboard для Worker
- [ ] Dashboard для Billing Contact
- [ ] Dashboard для Owner/Manager
- [ ] CRUD запросов на оплату
- [ ] Загрузка файлов через Telegram Bot API
- [ ] Real-time обновления через WebSockets
- [ ] Уведомления в Telegram из веба

## API эндпоинты

- `GET /` - Главная (редирект на /login или /dashboard)
- `GET /login` - Страница входа
- `POST /auth/telegram` - Обработка авторизации от Telegram
- `GET /logout` - Выход из системы
- `GET /dashboard` - Dashboard (требует авторизации)
- `GET /health` - Health check

## Разработка

### Включение debug режима

В `web/database.py` установите `echo=True` для просмотра SQL запросов:

```python
engine = create_async_engine(
    config.database_url,
    echo=True,  # <-- SQL логирование
    future=True,
    pool_pre_ping=True,
)
```

### Hot reload

FastHTML/Uvicorn поддерживает автоматическую перезагрузку при изменении файлов:

```bash
uvicorn web.app:app --reload --port 8000
```

## Troubleshooting

### Ошибка "STORAGE_CHAT_ID не найден"

Создайте приватную группу и добавьте бота. См. раздел "Как получить STORAGE_CHAT_ID" выше.

### Ошибка "Пользователь не найден в системе"

Пользователь должен быть создан в БД через:
- Команду `/start` в боте
- Или вручную через Owner интерфейс

### Telegram Login Widget не работает

1. Проверьте `data_telegram_login` - должно совпадать с именем бота
2. Убедитесь что домен настроен через @BotFather `/setdomain`
3. Для localhost может потребоваться настройка HTTPS (или использовать ngrok)

## Безопасность

- ✅ Проверка HMAC-SHA256 подписи от Telegram
- ✅ Сессии защищены secret_key
- ✅ Авторизация на каждом защищенном маршруте
- ✅ Валидация прав доступа по ролям

⚠️ **Важно**: В production используйте HTTPS и надежный `WEB_SECRET_KEY`!
